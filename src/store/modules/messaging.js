import { messaging } from '@/main';

const state = {
  test: "ok"
};

const getters = {

};

const mutations = {

};

const actions = {
};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
